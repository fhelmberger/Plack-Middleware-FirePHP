# auto-generated shipit config file.
steps = FindVersion, ChangeVersion, CheckChangeLog, Manifest, DistTestExtra, Commit, Tag, MakeDist, DistClean, UploadCPAN, Twitter

git.tagpattern = v%v
git.push_to = origin

twitter.config = ~/.twitterrc
